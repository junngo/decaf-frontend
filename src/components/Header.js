// src/components/Header.js

import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>My App</h1>
            <nav>
                {/* Navigation links */}
            </nav>
        </header>
    );
}

export default Header;
